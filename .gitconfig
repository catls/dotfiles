[color]
        ui = always
        branch = always
        diff = always
        interactive = always
        status = always
        grep = always
        pager = true
        decorate = always
        showbranch = always
[grep]
        lineNumber = true
[log]
        decorate = full
[include]
        path = ~/.private/private.gitconfig
